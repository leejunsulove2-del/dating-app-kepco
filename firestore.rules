rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 모바일, PC 등 어떤 단말기에서든 로그인 여부와 관계없이 
      // 모든 데이터를 자유롭게 읽고 쓸 수 있도록 방화벽을 완전히 개방합니다.
      allow read, write: if true;
    }
  }
}
